<h3 class="title">公司公告</h3>

<div class="posts">
  <ul>
    <% @posts.each do |post| %>
      <li>
        <span class="date"><%= post.created_at.strftime('%Y-%m-%d') %></span>
        <%= link_to post.title, post %>
      </li>
    <% end %>
  </ul>
</div>